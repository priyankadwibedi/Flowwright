# Local API data

Runtime SQLite databases may be created here for local development and are ignored by Git. The current deterministic invoice demo reads its checked-in fixtures from `packages/sample-workflows` and `examples/invoice-approval`; no uploaded media is persisted here.
